#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    char parola[30];
    printf("Inserisci una parola: ");
    scanf("%s", parola);

    for (int i = 0; i < strlen(parola); i++)
    {
        
    }
    
    return 0;
}
